Overview

Exercise Application is a full-stack mobile app built with React Native (Expo) for the frontend and Express.js for the backend. It allows users to log, track, and manage exercises/workouts through a RESTful API connected to a SQL database.

Frontend: React Native with Expo

Backend: Express.js server with REST API endpoints

Database: SQL (configured via Knex.js)
